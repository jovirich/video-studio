"""Asset pipeline.

`manifest` and `store` are implemented: a generated asset can be hashed, stored,
recorded, verified, and frozen. `conform` — the refusal that reads the manifest
back and blocks an unrecorded clip from reaching the edit — is NOT BUILT.

See ../../README.md maturity table and docs/status.md.
"""
